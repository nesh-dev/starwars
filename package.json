{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "engines": { "node": "14.5.0" },
  "scripts": {
    "start:dev": "lerna run start:dev --stream", 
    "codegen": "lerna run codegen", 
    "install": "lerna bootstrap --hoist && learna clean-y"
  }
}
