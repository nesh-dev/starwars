{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "engines": { "node": "14.5.0" },
  "scripts": {
    "start": "lerna run start:dev --stream", 
    "codegen": "lerna run codegen", 
    "install": "lerna exec -- npm install --production"
  }
}
